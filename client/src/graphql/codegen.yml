overwrite: true
schema: "http://localhost:8080/graphql"
documents: "src/graphql/**/*.graphql"
generates:
  src/graphql/generated/generated.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
hooks:
  afterAllFileWrite:
    - prettier --write --list-different --trailing-comma=all ../.
    - npx eslint src --fix
